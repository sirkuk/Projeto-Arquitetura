version: '3'

services:
  DFK.conta:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"
  DFK.service.conta:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"
networks:
  default:
    external:
      name: nat

